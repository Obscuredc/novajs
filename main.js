var canvas = document.getElementById( >> CANVAS NAME HERE <<);
var ctx = canvas.getContext('2d');

  /**
    1. Clear Canvas
    2. draw
    3. Check Collisions
      > test for collisions using IsCollision
      > calculate angle and apply to object
    4. calculate for next frame using calculateNextFramePosition, set x and y to the positions
  **/

function start () {
  //this code will run on start
  console.log("Hello, World!");
  
}

function update () {
  //this code will run every frame
  
  
}
